name: Pipeline

permissions:
  contents: read
  packages: write

on:
  push:
    branches:
      - main

jobs:
  ci:
    permissions:
      contents: read
    uses: ./.github/workflows/ci.yml

  cd:
    needs: ci
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/cd.yml