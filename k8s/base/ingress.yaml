apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: pipeline-project

spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix

            backend:
              service:
                name: pipeline-project

                port:
                  number: 80